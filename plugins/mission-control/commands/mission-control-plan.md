---
description: "Mission Control — plan: triage + mandatory recon + wave/lane design for a new program"
argument-hint: <objective>
---

Execute the **mission-control** skill in **plan** mode. First read `skills/mission-control/SKILL.md` — it is authoritative; this command is only an entry point.

Operator input: $ARGUMENTS

Follow §2 (plan) exactly: triage (one-shot vs program), the non-negotiable recon (true bases, active goals, open MRs, project facts note, box state if relevant), the resource/headroom check with derived caps, wave/lane design with owned files and the enablement sequence, at most one question round with recommendations marked, and the program note written to the vault. Load `references/prompt-anatomy.md` and `references/lessons.md` before designing anything.
