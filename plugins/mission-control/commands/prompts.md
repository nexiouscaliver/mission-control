---
description: "Mission Control — prompts: forge + red-team the wave's session prompts"
argument-hint: [wave]
---

Execute the **mission-control** skill in **prompts** mode. First read `skills/mission-control/SKILL.md` — it is authoritative; this command is only an entry point.

Operator input: $ARGUMENTS (a wave selector — optional; default is the next unblocked wave per the program note)

Follow §3 (prompts) exactly: read the program note + project facts notes, re-run the headroom check, forge each prompt per the anatomy grammar with every variable verified this turn, apply the RAM-derived caps, and pass every prompt through the red-team gate before emitting. Emit paste-ready blocks + launch steps + one goal statement per prompt, and append the prompt-log rows. `references/prompt-anatomy.md` is the grammar; `references/lessons.md` is the why.
