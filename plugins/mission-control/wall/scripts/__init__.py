"""Operator scripts for the mc-wall checkout (``python -m scripts.<name>``).

A docstring-only package marker: it exists so ``python -m scripts.mc_status``
resolves when invoked from the repo root. No code lives here.
"""
