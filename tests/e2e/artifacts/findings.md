# MC Wall live-browser E2E — findings (goal `mcwall-e2e`)

GATED_SHA: 2a6385f (closing full-tier gate ran on this SHA; the evidence commit that adds this file + PNGs is inert .png/.md only, recorded in the run's final report)
Method: real stack (mc_wall server + tower) booted in-process from the run worktree against REAL read-only inputs (live session db `~/.zcode/cli/db/db.sqlite`, real vault notes, real repos), temp MC_WALL_HOME per boot, test port 8799 (+8791..8793 for mock-state servers). Driven with the browser-use in-app browser; every screenshot read and judged by the run's vision model. Product code untouched (F-1/F-2 are findings, not fixes).

## Product findings

| ID | Severity | Summary | Where | Repro |
| --- | --- | --- | --- | --- |
| F-1 | P1 | Prod boot wiring broken: `/state` always 503 via `python -m mc_wall.server`. `_default_collect_state()` calls the tower argless while `collect_state(config)` requires a TowerConfig; no env/config override for tower inputs exists. The launchd path (`templates/run_sh.template`) can never serve a live board. | mc_wall/server/app.py:63-66 vs mc_wall/tower/collect.py:21 | Boot with temp home + wall.json → `GET /<token>/state` → 503 `collect_state_failed` (tests/server/test_mcwalls_entry.py:72-74 even accepts 503 as expected). |
| F-2 | P1 | Server cannot live longer than ~10 s. `start()` runs serve_forever as a DAEMON thread; `wait_shutdown()` is `serve_thread.join(timeout=10)`; `run_server()` then returns → interpreter exit kills the serve loop. launchd KeepAlive=CrashedOnly never restarts a clean exit-0. | mc_wall/server/app.py:749-753, 762-765, 911-918 | Boot the prod path; process exits ~10 s after READY. Observed live: harness mirror printed its STOPPED line ~10 s post-READY (fixed in harness only). |
| F-3 | P1 | Page→server POST surface dead in live mode: all three page POSTs omit `Content-Type: application/json` (fetch defaults to text/plain) and the server guard 415s any non-JSON content type. NEEDS ME NOW, activate-app, launch cancel/re-copy all fall into the page's "refused — page-side fallback" branch. | web/app.js:997, 1198, 1526 vs mc_wall/server/app.py:242-308 | Click NEEDS ME NOW on the live page → "needs-me-now refused — page-side fallback"; wall.log carries no `audit action=needs-me-now` line. curl with the header → 200. |
| F-3b | P1 | Server-side NEEDS ME NOW picker reads a key the tower never emits: `owed_actions(state)` reads `state["owed_actions"]` but the tower emits `verify_queue`/`human_actions` → picker always returns `None` ("audit action=needs-me-now row_id=-", zero skip lines) while the page counter shows 4v·0m. Both halves pass their own unit suites; only the joined system is wrong. | mc_wall/server/state_contract.py:38-42 vs mc_wall/tower/collect.py:54-63 | `POST /<token>/needs-me-now` with proper headers → `{"ok":true,"action":null}` on a board with 4 owed verifies. |
| F-4 | P2 | Degraded entries render twice: top-bar badge row AND an unstyled plain-text `role=alert` block below it, duplicated every render. | web (banner-strip), screenshots `s8-viewport/board-1280x800.png` | Any state with degraded entries (live board had 5). |
| F-5 | P3 | UNPARSED chips overflow: the red `UNPARSED:` label overlaps the chip's left border; long raw status cells fill/exceed the hatched oval (real vault cell W1-L3 is worst). | web/style.css chip treatment; screenshots `s2-chips/col1-solid-hatched.png`, `s5-qa/mock-full-stalled-unparsed.png` | Render any lane whose status cell is not in the closed vocabulary. |
| F-6 | P3 | Dashed `verify?` chip shows no visible corner dot on the live board, while mock dashed chips (push/mr) do carry amber dots — inconsistent dot application or sub-pixel dot at this size. | screenshots `s2-chips/col1-solid-hatched.png` vs `s5-qa/mock-full.png` | Compare lane chips with `verify?` vs `push:`/`mr:` chips. |

Observations (correct behavior, recorded for completeness):

- OBS-1: `POST /copy-goal` 404 `unknown-row` for W2 rows whose goal dirs lack forge `goal.md` (hand-launched runs bypass the mission-control forge) — correct refusal; manifest renders present with empty artifacts.
- OBS-2: `network degraded: git mc-wall` / `mr mc-wall` — the tower truthfully reports failed network signals for the mc-wall repo.
- OBS-3: pre-existing flaky test `tests/server/test_mcwalls_recovery.py::test_concurrent_launch_and_monitor_never_corrupt` (stochastic `assert 409 in statuses`, all-200 race) — reproduced with the harness absent; rerun-on-failure is the mitigation.

## Scenario verdicts (vision-model findings, per scenario)

| Scenario | Verdict | Evidence + notes |
| --- | --- | --- |
| S1 live board | PASS | `s1-live/board-default.png`, `s8-viewport/board-1280x800.png`. Three columns from REAL inputs; LIVE badge + green live dot; NEEDS ME NOW 4v·0m exactly matching state (4 verify rows, 0 merge rows); armed bar (seeded) with cancel; 5 honest degraded badges; "state 0s" age caption. mc-wall program card: 10 real lanes parsed from the real vault note (W1-L1/W1-L7/W0-L0/W1-L4R `done`, W2-L5/W2-L6 `forged` — W2-L6 is this very run; 4 compound status cells correctly UNPARSED). secfix/det-filter: `notes degraded · no lanes` — the real notes' tables are NOT in the tower grammar (secfix header `Wave/Lane/Package/Owned files/Status`, det-filter has no pipe table), so the overlay's "expect secfix/det-scan lanes with note statuses" is NOT met by the data; the board reports that truthfully. Sessions column: real groups + unmapped (16) incl. this run's own session at 0s. |
| S2 trust grammar | PASS | `s2-chips/col1-solid-hatched.png`, `s2-chips/col2-verify-derived.png`. SOLID green `done · stamped 2h` with filled green corner dot; DASHED amber derived chips with source+age inline (`push: ls-remote 900s`, `mr: !34 open 3600s` + green `!34` badge); HATCHED red UNPARSED with hollow-ring red corner dot — line style AND dot shape correspond at a glance. Verify-queue rows carry `signals: done·63294s` (source+age) and master hints. F-6: live `verify?` dashed chip shows no visible dot. |
| S3 NEEDS ME NOW | PASS-WITH-FINDING | Counter mix exact (4v·0m). Click copied the EXACT oldest-owed verify command to the clipboard (pbpaste == `/mission-control-verify sess_2243e9a1…` for W0-L0, oldest at 75.7 ks) — but via the page-side fallback because of F-3; the server action path is dead (F-3b: `action:null`). Armed bar exercised: seeded record recovered at boot (prompt-armed→await-birth), second boot goal-armed wording shown, server-side cancel propagated to the top bar ("✓ cleared — cancel") on the next poll without reload (`s3-needs-me-now/topbar-armed-cleared.png`). |
| S4 repo badges | PASS | `s5-qa/mock-full.png` + `s5-qa/mock-full-page.png`: GitLab `!34` and GitHub `#56` badges are visually distinct green chips inline with their dashed mr chips. Live truth: both configured repos are GitLab, so the live board shows no `#N` rows (badge distinction proven on the mock; no live GitHub row exists to photograph). |
| S5 QA/mock states | PASS (1 disclosed partial) | Mock docs served as live `/state` per the overlay's "file:// **or mock param**" (in-app browser cannot navigate file: URLs). `mock-full.png`: padlock on the `done` chip with precondition_mrs, `title pending` session placeholder, UNPARSED raw-text chips, banner with dismiss, v·m mix counter, master-mapped verify rows, `idle >24h (1)` collapsed group, disabled COPY VERIFY with "no verify_cmd (session not parsed)". `mock-freeze.png`: full-board hatch + "tracking degraded: session store unreadable" + dimmed disabled counter — freeze never serves last-good unlabeled. `mock-minimal-flagged-bar.png`: flagged armed-bar variant. Stalled-with-cause: DOM-located (`inactive for 21600s > stall_t 6h`) + covered by the page's 79/79 selftest, but no dedicated screenshot — IAB wheel-scroll timed out and fullPage stitching repeats viewports on the fixed-100vh grid (tool artifact) — LIMIT-1, disclosed. |
| S6 degradation live | PASS | SIGTERM'd the real server → within ~15-20 s (3 strikes × 5 s poll) the strip showed "wall server unreachable (last good 4m)" with dismiss, the live-dot flipped to `stale` (red in `s6-degradation/outage-banner-top.png`), caption "last-good Xm", panels holding last-good. Restarted with the SAME home/token → banner cleared and dot returned `live` ~1 s of first check (≤10 s of READY), with the pre-outage JS marker `alive-1789914627791` and the original navigation timeOrigin intact — provably no manual reload and no schema_version flap (`s6-degradation/recovered-top.png`). Caveat: IAB suspends background-tab timers, so strikes accrue only while the tab is foregrounded (environment, not product). |
| S7 console errors | PASS | Injected page-side collector (window errors, unhandled rejections, console.error) on every tab/scenario: `{"errors":[],"consoleErrors":[]}` through live rendering, the refused-POST fallbacks, and the full outage/recovery cycle. Network-layer entries excluded per the SC-8 classification; collector installs post-DOMContentLoaded (pre-injection errors theoretically uncounted — page is small, declared). |
| S8 1280×800 | PASS | `s8-viewport/board-1280x800.png`: all three columns fully visible at 1280×800; minor right-edge scrollbar overlap in col 3. |

## Coverage limitations (declared)

- LIMIT-1: no dedicated screenshot of the stalled-with-cause chip (DOM + selftest evidence only). LIMIT-2: file:// QA mode unreachable → badge shows LIVE rather than "QA · case: …"; qaArmCycle demo not exercised; poll-failure banner proven live in S6 instead. Mock docs not individually screenshotted: null-program, empty-lanes, no-schema-version, pending-null (chip states they exist to harden are all visually covered above; docs parsed in harness tests and by the page's 79/79 selftest). LIMIT-3: console collector installed after DOMContentLoaded. Clipboard checks performed via pbpaste in the orchestrator shell (macOS pbcopy/pbpaste, per the product's own mechanical-action design).

## Cleanup statement

All servers stopped (ports 8791/8792/8793/8799 free at T-end), all temp MC_WALL_HOMEs swept (`/tmp/mcwall-e2e*`). No product file, gate config, or vault/session-db write was made by this run; the session db was opened read-only by the tower and by explicit `mode=ro` probes.
